Updated: 2026-09-17

## Current focus
Nothing in flight. The annotation realignment is committed, the suite and docs are re-baselined,
and the host is clean - no cluster, containers or volumes (deploy/.env is regenerated by jit-up).

## Blocked
- Nothing.

## Done (verified)
- addcf88 fix(app): annotations now name their consumers - vote redis+pgadmin, worker
  redis+postgres, result postgres; J4/J6/J7/J9 and six docs realigned in the same commit.
- Suite live before the teardown: demo-up R1-R17 17 PASS / 0 FAIL, jit-verify J1-J11 11 PASS /
  0 FAIL. J6 swept pgadmin in 155s and left redis+postgres running.
- 39597af adds docs/console-demo-test-plan.md; a70f832 records the run logs and the teardown.
- Host clean: k3d clusters, module containers and volumes all gone. The k3d-voting-app docker
  network is left on purpose - verify-jit.sh J1 fails if it is missing.

## Checkpoints
- S18 PASS (amended) at eecb02e. S19, S20, S21 ticked + CLEAR. S17 untouched.

## Next step
Decide the J8 volume assertion (Watch out) - it needs a ~20 min suite run to re-baseline - and
whether to commit console/test_console.py and docs/TESTING-THE-CONSOLE.md, both still untracked.

## Watch out
- pgadmin's lease is on vote deliberately: no workload consumes it, and moving it breaks both
  the orphaning demo and voting-b's replace-on-missing-path patch.
- Nothing asserts that a destroy REMOVES the postgres data volume any more: J6 now asserts it
  stays (postgres was not swept) and J8 never checked volumes. Verified by hand after J8
  (voting-b's volume gone), but ungated.
- Nothing is pushed: local main is 3 ahead of origin/main.
- refs/cline/checkpoints/* snapshot .env and terraform.tfstate; never `git push --all`/`--mirror`.
